name: Mirror to GitLab

on:
  push:
    branches:
      - main

jobs:
  mirror:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Push to GitLab
        env:
          GITLAB_TOKEN: ${{ secrets.GPAT }}
        run: |
          git remote add gitlab https://oauth2:${GITLAB_TOKEN}@gitlab.fullstackacademy.sk/opoiasnik/FSA-FE.git
          git push --mirror gitlab
